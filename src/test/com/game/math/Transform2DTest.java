package test.com.game.math;
/*
import main.com.game.math.Transform2D;
import main.com.game.math.Vector2D;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class Transform2DTest {
    private static final double FLOATING_POINT_DELTA = 0.1;

    @Test
    void getEndPosition() {
        Transform2D transform = new Transform2D();
        transform.setPosition(new Vector2D(10.0, 10.0));
        transform.setScale(new Vector2D(10.0, 10.0));

        Vector2D output = transform.getEndPosition();

        assertEquals(20.0, output.x, FLOATING_POINT_DELTA);
        assertEquals(20.0, output.y, FLOATING_POINT_DELTA);
    }

    @Test
    void getCenterPosition() {
        Transform2D transform = new Transform2D();
        transform.setPosition(new Vector2D(10.0, 10.0));
        transform.setScale(new Vector2D(10.0, 10.0));

        Vector2D output = transform.getCenterPosition();

        assertEquals(15.0, output.x, FLOATING_POINT_DELTA);
        assertEquals(15.0, output.y, FLOATING_POINT_DELTA);
    }
}*/